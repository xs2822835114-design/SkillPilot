# ===== SkillMap 前端环境变量模板（复制为 .env 后按需修改）=====
# 仅含前端可见变量，勿放置密钥

# 后端直接地址（若已配置网关代理，可保持空/同源，走 vite proxy）
VITE_API_BASE_URL=

# 开发代理目标：本地后端 Flask 地址（macOS AirPlay 占用 5000 时改用实际端口）
VITE_DEV_PROXY_TARGET=http://localhost:8081

# 开发态默认用户
VITE_DEFAULT_USER_ID=U10001